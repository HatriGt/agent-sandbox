{
  "name": "agent-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Delegate a Cursor task to an isolated microsandbox microVM and run Claude Code inside it.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-sandbox-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
